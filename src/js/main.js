// Import main SCSS file (which includes Bootstrap customization)
import '../scss/styles.scss';

// Import Bootstrap JS
import * as bootstrap from 'bootstrap';

// Optionally import specific components
// import { Tooltip, Toast, Popover } from 'bootstrap';